# Docker Theory Notes

## What is Docker?

Docker is a platform for running applications in isolated environments called containers.

## Why Docker is useful

- Same environment on different machines
- Easy application deployment
- Isolation from the host system
- Fast start compared to virtual machines
- Useful for CI/CD and DevOps workflows

## Image

An image is a template used to create containers.

Example:

```bash
docker pull nginx
